.next
node_modules
coverage

# npm rewrites this itself; formatting it only creates churn.
package-lock.json

# Regenerated by `npm run db:types`; formatting it only makes it drift back.
src/lib/database.types.ts

# Already applied to a hosted database. Reformatting applied SQL buys nothing.
supabase/migrations

# Prettier rewraps prose, which would rewrite every doc for no benefit and
# lose the line breaks that keep them readable in a diff.
*.md
